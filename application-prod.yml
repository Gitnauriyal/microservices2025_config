spring:
  application: 
    name: Congif-Prod
eureka:
   instace:
       prefer-ip-address: true
   client: 
       fetch-registry: true
       register-with-eureka: true
       service-url.defaultZone: http://localhost:8761/eureka
